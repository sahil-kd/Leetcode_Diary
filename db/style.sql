-- This script runs automatically at each db session to style the table in terminal --> for now not working
-- .read db/style.db --> run this inside sqlite3 session to execute/apply this style script
.mode column
.headers on
.width 20
.separator "---------------------"